(*This is a block comment.
It spans multiple lines.*)

(*To declare variables, functions, and else, use "let", with 2 semicolons.*)
let myInt = 80;;
let myString = "Hello World!";;

(*OCaml is very similar to python, with lists, tuples, and the like.*)
